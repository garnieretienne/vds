%article.publication
  %header
    = image_tag publication.thumbnail, class: "publication_thumbnail" if publication.thumbnail?
    %h1= link_to publication.title, publication
    .publication_date= l(publication.created_at, format: :short)
  = publication.to_html